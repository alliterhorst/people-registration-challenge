{
  "name": "people-registration-challenge",
  "scripts": {
    "start:dev": "docker compose -f docker-compose.yml up --build",
    "start:prod": "docker compose -f docker-compose.prod.yml up --build"
  }
}
